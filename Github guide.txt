40039561+sahil-web-dev@users.noreply.github.com

1)git clone URL_OF_REPOSITORY Directory_where_to_download

	----This copies/downloads the repository from GitHub.

2)git add File_Name

	----This makes the files to stage for upload.

3)git commit -m "MESSAGE(whats the cahnges"

	----This uploads the file on our HOST with messgae.

4)git push origin master

	----This uploads the file to the SERVER_GIT_HUB with origin of master.

5)